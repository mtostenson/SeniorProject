subprojects {
	apply plugin: 'java'
}

project('BackTalkServer') {
	dependencies {
		compile project(':BackTalkPackets')
	}
}

project('BackTalkTestClient') {
	dependencies {
		compile project(':BackTalkPackets')
	}
}
